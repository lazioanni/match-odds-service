package com.match.service.enums;


/**
 * Specifier is an enum that represents the specifier types.
 *
 * @author lioannidis
 * @version 0.1
 */
public enum Specifier {
    X,
    ONE,
    TWO,
    OVER,
    UNDER
}
